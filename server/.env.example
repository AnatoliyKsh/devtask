# DevFlow Server configuration
PORT=3001
# Path to SQLite DB file (relative or absolute). Default: ./data/devflow.db
DB_PATH=./data/devflow.db
# If you build the client, the server can serve it when NODE_ENV=production
NODE_ENV=development
